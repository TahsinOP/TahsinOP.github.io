source "https://rubygems.org"

# Mirrors exactly what GitHub Pages runs, so `bundle exec jekyll serve`
# locally === what github.io publishes. No custom plugins, no Actions needed.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"   # required on Ruby >= 3.0
